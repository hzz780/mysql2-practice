{
  "name": "mysql-only",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pm2": "pm2 start index.js --name mysql-only --exp-backoff-restart-delay=100000 --time"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.9.1"
  }
}
